package org.firstinspires.ftc.teamcode.util.decodeutil;

import com.pedropathing.geometry.BezierCurve;
import com.pedropathing.geometry.BezierLine;
import com.pedropathing.geometry.Pose;
import com.pedropathing.paths.Path;
import com.pedropathing.paths.PathChain;

import java.util.ArrayList;

public class Mirrorer {
    // mirrors a PathChain. work in progress
//    public static PathChain mirror(PathChain p) {
//        ArrayList<BezierCurve> paths = new ArrayList<>();
//        for (int i = 0; i < p.size(); i++) {
//            Path path = p.getPath(i);
//            ArrayList<Pose> newPoses = new ArrayList<>();
//            for (Pose controlPoints: path.getControlPoints()) {
//                newPoses.add(controlPoints.mirror());
//            }
//            path.getHeadingInterpolator()
//            if (newPoses.size() < 3) {
//                paths.add(new BezierLine(newPoses.get(0), newPoses.get(1))
//            } else {
//                paths.add(new BezierCurve()
//            }
//
//        }
//    }
}
